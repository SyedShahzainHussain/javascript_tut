// ! textContent and innerText
// ? textContent is show the all text if they are visible or not
// ? innerText is show only visible text



const textID = document.getElementById("main-heading");

// const textContent = textID.textContent;

// console.log(textID.textContent);
// textID.textContent = "This is a new content";

// console.log(textID.textContent);

console.log(textID.textContent);
console.log(textID.innerText);

