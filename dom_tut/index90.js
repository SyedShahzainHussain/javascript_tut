// ! querySelector  And  querySelectorAll
// ? if u want an id you can called like this #id in querySelector
// ? if u want an class you can called like this .class in querySelector
// ? if u want an multiple class  with the same you can call  querySelectorAll();

//  const text = document.querySelector("#main-heading"); // ? getting id
// const button = document.querySelector(".headline"); // ? getting class
// console.log(text);
// console.log(button);



const navItem = document.querySelectorAll(".nav-item");  // ? is give the nodeList is not an array later cover this
console.log(navItem);