// ! insertAdjacentHTML(where, html)

// ? beforebegin
// ? beforeend
// ? afterbegin
// ? afterend


const todoList = document.querySelector(".todo-list");

todoList.insertAdjacentHTML("afterbegin","<li>hello<li/>")
