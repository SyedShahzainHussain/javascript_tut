// ! TDZ ( Temporal Death Zone ) 

// ! let and const are also hoisted yeh bhi memory ma store hote hai lekin uninitialized hote hai

// ? 1.  compilations -- > errors
// ? 1.  compilations -- > scope


// ? 2. code execution -- > in order to execute js code js create execution context;
// ? . creating phase
// ? . line by line code execution

